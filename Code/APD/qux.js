// Fixes the document thing, but doesn't fix always saying "image 1"
renderer1.domElement.addEventListener('mousedown', onDocumentMouseDown);
renderer2.domElement.addEventListener('mousedown', onDocumentMouseDown);
